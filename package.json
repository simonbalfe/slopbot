{
  "name": "slopbot-monorepo",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "docker compose up -d --build && bun run --filter @slopbot/web dev",
    "start": "bun run build:web && bun run start:server",
    "start:server": "bun run --filter @slopbot/server start",
    "check": "bun run --filter '*' check",
    "test": "bun test",
    "build": "bun run build:web",
    "build:web": "bun run --filter @slopbot/web build",
    "install:leads": "mkdir -p data/bin && docker run --rm -v \"$REPOS_ROOT/projects/clients/david/gmaps-leads-cli/apps/cli:/src:ro\" -v \"$PWD/data/bin:/out\" -w /src golang:1.25 go build -o /out/leads ."
  },
  "devDependencies": {
    "bun-types": "1.3.5",
    "typescript": "5.9.3"
  }
}
